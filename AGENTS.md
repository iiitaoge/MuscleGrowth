# Project Notes

- This is a Roblox project using Rojo.
- Keep `src` organized by Roblox service names, matching `default.project.json`.
- Use `.server.lua` for `Script`, `.client.lua` for `LocalScript`, and `.lua` for `ModuleScript`.
- Prefer small, focused changes and avoid moving assets between services unless the Rojo mapping is updated too.

# 和我交互的规则

- 让我的Roblox引擎能力获得增长
- 总是让我去考虑如何提升性能和防止玩家作弊
- 总是让我去思考如何写出更容易维护的代码
- 总是让我去思考如何写出更具扩展性的代码
